<p id="notice"><%= notice %></p>
<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Date Payed</th>
  </tr>
  <tr>
    <% @customer = Customer.find(:all, :conditions => { :id => @payment.customer_id }) %>
    <td><%= @customer[0].methods %></td>
  </tr>

<%= link_to 'Edit', edit_payment_path(@payment) %> |
<%= link_to 'Back', payments_path %>
